*.iml
.gradle
/local.properties
/.idea
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
/output
/.kotlin
/cmake/build*

# Tooling / AI assistants
.github/
.gemini/
.claude/
.beans/
.beans.yml
CLAUDE.md

# Design / working files
New UI/

# Pre-existing vendored third-party repos
third_party/secret-service/

# translate-kit build tree: half a gigabyte of upstream git history, rebuilt by
# scripts/translatekit/fetch-and-patch.sh. Only the AAR it produces is committed
# (third_party/translatekit/), which is what the app actually needs.
third_party/translatekit-src/

# Prebuilt native libraries
**/androidMain/jniLibs/**/*.so
.worktrees

# Personal migration artifacts
sipurraP-data.tar
*.b64
v2.sqlite
stable.sqlite

# Similar-series bench: local scratch index pulled from the server
scripts/similar-bench/index_*.json
__pycache__/
